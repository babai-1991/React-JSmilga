const Book = (props) => {
	const { bookCover, bookName, authorName } = props;

	return (
		<article className="book">
			<img src={bookCover} />
			<h1>{bookName}</h1>
			<h4>-{authorName}</h4>
			{props.children}
		</article>
	);
};